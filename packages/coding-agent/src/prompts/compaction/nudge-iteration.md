Many operations have been performed since the last user message. Consider compressing completed phases of work using the `compress` tool.

Target: investigation phases that reached conclusions, tool outputs that have been processed, or exploration paths that were abandoned. Use `mNNNN` message IDs for the range.
