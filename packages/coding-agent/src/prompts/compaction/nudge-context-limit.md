CRITICAL: Context window is near capacity. You **MUST** use the `compress` tool immediately to free space.

The `compress` tool takes three parameters:
- `startId`: The mNNNN ID of the first message in the range to compress
- `endId`: The mNNNN ID of the last message in the range
- `summary`: A detailed technical summary replacing the compressed content

Select the largest contiguous block of completed work (resolved research, finished implementations, dead-end explorations) and compress it now. Prioritize older content. Your summary must be exhaustive — capture all file paths, decisions, constraints, and findings.

Failure to compress immediately will cause context overflow and degraded performance.
