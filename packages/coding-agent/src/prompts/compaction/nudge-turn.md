Context is growing. Consider using the `compress` tool to condense completed sections.

Look for: resolved explorations, finished research, old tool outputs no longer needed. Use `mNNNN` message IDs to specify the range. Write thorough summaries that preserve key findings.
